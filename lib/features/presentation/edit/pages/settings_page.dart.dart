import 'package:flutter/material.dart';
    
class SettingsPageDart extends StatefulWidget {
  const SettingsPageDart({Key? key}) : super(key: key);

  @override
  _SettingsPageDartState createState() => _SettingsPageDartState();
}

class _SettingsPageDartState extends State<SettingsPageDart> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}